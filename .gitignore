# deps
node_modules/
.pnpm-store/

# build
dist/
build/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# expo
.expo/
.expo-shared/
web-build/

# prisma
packages/db/src/generated/

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage/
.gstack/
silvia-erp-backup-*.json
belasis-reference/.auth-profile*

# local uploads (dev fallback when S3 is not configured)
apps/api/uploads/

# Artefatos de raspagem do Belasis (399 MB): HTML capturado, frames de vídeo e
# estado de navegador. Ficam no disco para consulta, mas NUNCA no repositório —
# `_out/agenda-belasis-scrape/belasis-auth.json` guarda cookies de sessão do
# belasis.app, e credencial em git é permanente mesmo depois de apagada.
belasis-reference/_out/
